---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tokens/globals.css
  - .claude/rules/tokens.md
autonomous: true
requirements:
  - TOKN-01
  - TOKN-02
  - TOKN-03
  - TOKN-04
  - TOKN-05
  - RFMT-01
  - RFMT-03
  - RFMT-04

must_haves:
  truths:
    - "All color values are CSS custom properties — no hex, rgb, or oklch literals appear in any rule file"
    - "tokens/globals.css defines the complete token vocabulary with direct (single-hop) value assignments"
    - "The tokens.md rule file stays within 120 lines and lists the top 10-12 most-used tokens inline"
    - "Every critical token rule has an inline WHY comment explaining its rationale"
    - "Dashboard-specific extension tokens (--chart-6, --kpi-bg, --kpi-positive, --kpi-negative, --table-row-hover) are defined"
    - "The escape hatch for legitimate token exceptions is documented in tokens.md"
  artifacts:
    - path: "tokens/globals.css"
      provides: "Complete CSS custom property definitions, single-hop values only"
      contains: ":root {"
    - path: ".claude/rules/tokens.md"
      provides: "Token usage rules for AI — what tokens exist and how to reference them"
      contains: "paths:"
  key_links:
    - from: ".claude/rules/tokens.md"
      to: "tokens/globals.css"
      via: "explicit file reference in the rule text"
      pattern: "tokens/globals.css"
---

<objective>
Define the complete design token vocabulary for shadcn/ui dashboard projects.

Purpose: Tokens are the foundation everything else references. Components rules, page templates, and AI-generated code all reference token names. Without a stable, named token vocabulary, AI falls back to hardcoded hex values.

Output:
- `tokens/globals.css` — CSS `:root` block with all shadcn/ui base tokens + dashboard-specific extensions. Uses oklch() values per shadcn/ui v4. Single-hop only — no `var(--x)` referencing another `var(--y)`.
- `.claude/rules/tokens.md` — Path-scoped rule file AI reads when working on .tsx and .css files. Lists the token usage rules, the top 10-12 most-used token names inline, and the escape hatch for edge cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key decisions the executor MUST honor. Source: 01-CONTEXT.md -->

D-01: Use shadcn/ui's existing CSS variable system as the base token layer. Do NOT invent a custom token system.
D-02: Extend with dashboard-specific tokens ONLY where shadcn defaults are insufficient.
D-03: Tokens must be short and directly referenced by name. NO multi-hop chains (var(--x) referencing var(--y)).
D-13: Rule file budget is ~120 lines / ~1,500 tokens per file.
D-14: Imperative tone in English ("DO use...", "NEVER use...").
D-16: Every critical rule includes inline WHY comment.

shadcn/ui v4 uses oklch() color format (not hsl). All token values must use oklch().

shadcn/ui v4 base variable names (do NOT rename these):
  --background, --foreground, --card, --card-foreground, --popover, --popover-foreground
  --primary, --primary-foreground, --secondary, --secondary-foreground
  --accent, --accent-foreground, --muted, --muted-foreground, --destructive
  --border, --input, --ring
  --chart-1 through --chart-5
  --sidebar, --sidebar-foreground, --sidebar-primary, --sidebar-primary-foreground
  --sidebar-accent, --sidebar-accent-foreground, --sidebar-border, --sidebar-ring
  --radius, --radius-sm, --radius-md, --radius-lg, --radius-xl, --radius-2xl, --radius-3xl, --radius-4xl

Dashboard extension tokens to ADD (not in shadcn defaults):
  --chart-6          — 6th chart series color
  --kpi-bg           — KPI card background (alias to --card initially)
  --kpi-positive     — Positive delta indicator (green)
  --kpi-negative     — Negative delta indicator (red)
  --table-row-hover  — Table row hover (alias to --accent initially)

Tailwind v4 uses @theme {} CSS block (NOT tailwind.config.js — that is deprecated).
Typography: Use Tailwind's built-in type scale (text-sm, text-base, etc.) directly in rules.
Custom typography CSS vars are low-value — AI training data does not reliably know them.

Rule file frontmatter format (verified from official Claude Code docs):
  ---
  paths:
    - "src/**/*.{tsx,css}"
    - "app/**/*.{tsx,css}"
  ---
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tokens/globals.css with complete CSS custom property definitions</name>
  <files>tokens/globals.css</files>

  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (sections: "shadcn/ui v4 CSS Variable Reference", "Pattern 4: Token Definition Order", "Dashboard-Specific Extensions")
  </read_first>

  <action>
Create the file `tokens/globals.css`. This file is the single source of truth for all design tokens.

Structure the file in this exact order, with each group clearly labeled with a comment:

```css
/* tokens/globals.css
   Design token definitions for shadcn/ui dashboard projects.
   All values use oklch() per shadcn/ui v4.
   Single-hop only — no var(--x) referencing another var(--y).
   Reference tokens by name in all component and page code.
*/

@layer base {
  :root {
    /* === BACKGROUND & SURFACE === */
    --background: oklch(1 0 0);
    --foreground: oklch(0.145 0 0);
    --card: oklch(1 0 0);
    --card-foreground: oklch(0.145 0 0);
    --popover: oklch(1 0 0);
    --popover-foreground: oklch(0.145 0 0);

    /* === ACTION & BRAND === */
    --primary: oklch(0.205 0 0);
    --primary-foreground: oklch(0.985 0 0);
    --secondary: oklch(0.97 0 0);
    --secondary-foreground: oklch(0.205 0 0);
    --accent: oklch(0.97 0 0);
    --accent-foreground: oklch(0.205 0 0);
    --muted: oklch(0.97 0 0);
    --muted-foreground: oklch(0.556 0 0);
    --destructive: oklch(0.577 0.245 27.325);

    /* === UI STRUCTURE === */
    --border: oklch(0.922 0 0);
    --input: oklch(0.922 0 0);
    --ring: oklch(0.708 0 0);

    /* === CHART PALETTE (extendable) === */
    --chart-1: oklch(0.646 0.222 41.116);
    --chart-2: oklch(0.6 0.118 184.704);
    --chart-3: oklch(0.398 0.07 227.392);
    --chart-4: oklch(0.828 0.189 84.429);
    --chart-5: oklch(0.769 0.188 70.08);
    --chart-6: oklch(0.55 0.18 280);

    /* === SIDEBAR (dashboard-relevant) === */
    --sidebar: oklch(0.985 0 0);
    --sidebar-foreground: oklch(0.145 0 0);
    --sidebar-primary: oklch(0.205 0 0);
    --sidebar-primary-foreground: oklch(0.985 0 0);
    --sidebar-accent: oklch(0.97 0 0);
    --sidebar-accent-foreground: oklch(0.205 0 0);
    --sidebar-border: oklch(0.922 0 0);
    --sidebar-ring: oklch(0.708 0 0);

    /* === RADIUS SCALE === */
    --radius: 0.625rem;
    --radius-sm: calc(var(--radius) - 4px);
    --radius-md: calc(var(--radius) - 2px);
    --radius-lg: var(--radius);
    --radius-xl: calc(var(--radius) + 4px);
    --radius-2xl: calc(var(--radius) + 8px);

    /* === DASHBOARD EXTENSIONS === */
    /* KPI card background — matches card surface by default */
    --kpi-bg: oklch(1 0 0);
    /* Delta indicators — positive (green) and negative (red) */
    --kpi-positive: oklch(0.55 0.18 142);
    --kpi-negative: oklch(0.6 0.22 25);
    /* Table row hover — matches accent by default */
    --table-row-hover: oklch(0.97 0 0);
  }

  .dark {
    /* Dark mode counterparts — same token names, darker oklch values */
    --background: oklch(0.145 0 0);
    --foreground: oklch(0.985 0 0);
    --card: oklch(0.205 0 0);
    --card-foreground: oklch(0.985 0 0);
    --popover: oklch(0.205 0 0);
    --popover-foreground: oklch(0.985 0 0);
    --primary: oklch(0.922 0 0);
    --primary-foreground: oklch(0.205 0 0);
    --secondary: oklch(0.269 0 0);
    --secondary-foreground: oklch(0.985 0 0);
    --accent: oklch(0.269 0 0);
    --accent-foreground: oklch(0.985 0 0);
    --muted: oklch(0.269 0 0);
    --muted-foreground: oklch(0.708 0 0);
    --destructive: oklch(0.704 0.191 22.216);
    --border: oklch(1 0 0 / 10%);
    --input: oklch(1 0 0 / 15%);
    --ring: oklch(0.556 0 0);
    --chart-1: oklch(0.488 0.243 264.376);
    --chart-2: oklch(0.696 0.17 162.48);
    --chart-3: oklch(0.769 0.188 70.08);
    --chart-4: oklch(0.627 0.265 303.9);
    --chart-5: oklch(0.645 0.246 16.439);
    --chart-6: oklch(0.65 0.18 280);
    --sidebar: oklch(0.205 0 0);
    --sidebar-foreground: oklch(0.985 0 0);
    --sidebar-primary: oklch(0.488 0.243 264.376);
    --sidebar-primary-foreground: oklch(0.985 0 0);
    --sidebar-accent: oklch(0.269 0 0);
    --sidebar-accent-foreground: oklch(0.985 0 0);
    --sidebar-border: oklch(1 0 0 / 10%);
    --sidebar-ring: oklch(0.556 0 0);
    --kpi-bg: oklch(0.205 0 0);
    --kpi-positive: oklch(0.65 0.18 142);
    --kpi-negative: oklch(0.65 0.22 25);
    --table-row-hover: oklch(0.269 0 0);
  }
}
```

CRITICAL RULES for this file:
- Every token value must be a direct oklch() literal — never `var(--other-token)` (except for --radius-sm/md/lg/xl which use calc() on --radius — that is acceptable)
- Do NOT add any tokens beyond what is listed above
- Do NOT use hsl() format — shadcn/ui v4 uses oklch() exclusively
- Comments explain group purpose, not individual token values (keep file scannable)
  </action>

  <verify>
    <automated>grep -c "^    --" /Users/eyelove/workspace/opensource/shadcn-rules/tokens/globals.css</automated>
  </verify>

  <acceptance_criteria>
    - `tokens/globals.css` exists at project root under `tokens/` directory
    - `grep -c "^    --" tokens/globals.css` returns a number between 30 and 45 (token count is bounded)
    - `grep "hsl(" tokens/globals.css` returns 0 results (no hsl values — oklch only)
    - `grep "var(--" tokens/globals.css | grep -v "calc(" | grep -v "radius"` returns 0 results (no multi-hop chains except radius calc)
    - `grep -- "--chart-6" tokens/globals.css` finds the dashboard extension token
    - `grep -- "--kpi-bg" tokens/globals.css` finds the KPI background token
    - `grep -- "--kpi-positive" tokens/globals.css` and `grep -- "--kpi-negative" tokens/globals.css` both find results
    - `grep -- "--table-row-hover" tokens/globals.css` finds the table hover token
    - File contains both `:root {` and `.dark {` blocks
  </acceptance_criteria>

  <done>tokens/globals.css exists with complete shadcn/ui v4 base variables + dashboard extensions, all in oklch() format, no multi-hop chains, bounded at ≤ 45 tokens total.</done>
</task>

<task type="auto">
  <name>Task 2: Author .claude/rules/tokens.md — the AI-facing token usage rule file</name>
  <files>.claude/rules/tokens.md</files>

  <read_first>
    - tokens/globals.css (just created — read to extract the top tokens for inline listing)
    - .planning/phases/01-foundation/01-RESEARCH.md (section: "Token Rules File Structure", "Common Pitfalls" pitfall 4)
  </read_first>

  <action>
Create the file `.claude/rules/tokens.md`. This is the rule file AI reads when working on TSX or CSS files.

Requirements (per D-13, D-14, D-16, RFMT-01, RFMT-03, RFMT-04):
- YAML frontmatter with `paths:` targeting TSX and CSS files
- Under 120 lines total (enforce ruthlessly — cut prose, keep rules and token names)
- Imperative English tone throughout ("NEVER use...", "DO use...", "ALWAYS reference...")
- Every critical rule has an inline WHY comment
- Escape hatch documented for legitimate exceptions

Write the file with this exact structure:

```markdown
---
paths:
  - "src/**/*.{tsx,css}"
  - "app/**/*.{tsx,css}"
  - "components/**/*.{tsx,css}"
---

# Token Rules

All color, spacing, radius, and shadow values MUST use CSS custom property tokens.
NEVER hardcode hex, rgb, oklch, or Tailwind color primitives (gray-100, zinc-800, etc.) in component or page code.
// WHY: Raw values break theming and dark mode. Token names are stable; oklch values can change.

## Color Tokens — Use These

Reference tokens as Tailwind utility classes where available, or as `var(--token-name)` in CSS:

**Surfaces:**
`bg-background` · `bg-card` · `bg-popover` · `bg-muted` · `bg-accent`

**Text:**
`text-foreground` · `text-card-foreground` · `text-muted-foreground` · `text-primary-foreground`

**Actions:**
`bg-primary` · `bg-secondary` · `bg-destructive` · `border-border` · `ring-ring`

**Dashboard extensions (use `var()` syntax — no Tailwind utility class):**
`var(--chart-1)` through `var(--chart-6)` — chart series colors
`var(--kpi-bg)` — KPI card background
`var(--kpi-positive)` — positive delta (green)
`var(--kpi-negative)` — negative delta (red)
`var(--table-row-hover)` — table row hover

Full token list: `tokens/globals.css`

## Radius Tokens

`rounded-[--radius]` · `rounded-[--radius-sm]` · `rounded-[--radius-lg]`
NEVER use `rounded-md`, `rounded-lg` (Tailwind fixed values) — use the token-based form above.
// WHY: Project radius can be customized via --radius without changing all component classes.

## Typography

Font sizes: `text-xs` · `text-sm` · `text-base` · `text-lg` · `text-xl` · `text-2xl`
Font weights: `font-normal` (body) · `font-medium` (labels, captions) · `font-semibold` (headings, KPI values)
// WHY: Tailwind's type scale is in AI training data; custom --font-* tokens are not reliably known.

NEVER use inline `style={{ fontSize: "..." }}` or `style={{ fontWeight: "..." }}`.

## Spacing

Use Tailwind spacing utilities: `p-4`, `px-6`, `gap-4`, `space-y-6`.
NEVER use `style={{ padding: "..." }}` or `style={{ marginTop: "..." }}`.
// WHY: Tailwind's spacing scale produces consistent density; inline style bypasses all constraints.

Semantic spacing conventions:
- Component internal padding: `p-4` or `p-6`
- Between sibling components: `gap-4` or `gap-6`
- Between page sections: `space-y-8` or `gap-8`

## Forbidden Patterns

```tsx
// FORBIDDEN — hardcoded color
<div style={{ backgroundColor: "#1a1a2e" }}>
<div className="bg-[#1a1a2e]">
<div className="bg-zinc-900">

// FORBIDDEN — raw oklch/rgb
style={{ color: "oklch(0.5 0.2 240)" }}

// CORRECT
<div className="bg-background">
<div className="bg-card">
```

## Escape Hatch

If a token does not exist for a legitimate design need (e.g., a one-off data visualization color):
1. Add the token to `tokens/globals.css` first with a comment explaining its purpose
2. THEN reference it via `var(--your-new-token)` in component code
3. NEVER use an inline literal and promise to "add the token later"
```

After writing the file, verify it is under 120 lines with: `wc -l .claude/rules/tokens.md`
If over 120 lines, trim prose — every line must earn its place. Rules and token names take priority over explanation.
  </action>

  <verify>
    <automated>wc -l /Users/eyelove/workspace/opensource/shadcn-rules/.claude/rules/tokens.md</automated>
  </verify>

  <acceptance_criteria>
    - `.claude/rules/tokens.md` exists
    - `wc -l .claude/rules/tokens.md` returns 120 or fewer
    - `head -5 .claude/rules/tokens.md` shows `---` (YAML frontmatter present)
    - `grep "paths:" .claude/rules/tokens.md` finds the path-scoping field
    - `grep "\\.tsx" .claude/rules/tokens.md` finds tsx path pattern
    - `grep "\\.css" .claude/rules/tokens.md` finds css path pattern
    - `grep "WHY" .claude/rules/tokens.md | wc -l` returns 3 or more (WHY comments present on critical rules)
    - `grep "Escape Hatch\|escape hatch" .claude/rules/tokens.md` finds the escape hatch section
    - `grep "tokens/globals.css" .claude/rules/tokens.md` finds the reference to the source-of-truth file
    - `grep "NEVER\|FORBIDDEN" .claude/rules/tokens.md | wc -l` returns 4 or more (imperative prohibitions present)
    - `grep "chart-6\|kpi-bg\|kpi-positive\|kpi-negative\|table-row-hover" .claude/rules/tokens.md` finds dashboard extension tokens listed inline
  </acceptance_criteria>

  <done>`.claude/rules/tokens.md` is a complete, path-scoped token rule file under 120 lines that lists the top tokens inline, forbids raw values with WHY comments, and documents the escape hatch procedure.</done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks from the project root:

```bash
# Token count must be bounded
grep -c "^    --" tokens/globals.css

# No hsl() format — v4 uses oklch only
grep "hsl(" tokens/globals.css && echo "FAIL: hsl found" || echo "PASS"

# No multi-hop chains (except radius calc)
grep "var(--" tokens/globals.css | grep -v "calc(" | grep -v "radius" && echo "FAIL: multi-hop found" || echo "PASS"

# Rule file must be under 120 lines
wc -l .claude/rules/tokens.md

# No className in token rule file (className is a component concern, not token concern)
grep "className" .claude/rules/tokens.md && echo "WARN: className in token rules" || echo "PASS"

# Dashboard extensions present
grep -- "--kpi-bg\|--kpi-positive\|--kpi-negative\|--table-row-hover\|--chart-6" tokens/globals.css | wc -l
```

All checks must pass before this plan is complete.
</verification>

<success_criteria>
1. `tokens/globals.css` contains the complete token vocabulary (shadcn/ui base + dashboard extensions) in oklch() format, single-hop values only
2. `.claude/rules/tokens.md` is path-scoped to TSX and CSS files, under 120 lines, with inline WHY comments on every critical rule
3. Both files can be read side-by-side: tokens.md references globals.css, globals.css has all tokens that tokens.md mentions
4. `grep "hsl(" tokens/globals.css` returns nothing (no legacy format)
5. `grep "className" .claude/rules/tokens.md` returns nothing (no scope leak)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
