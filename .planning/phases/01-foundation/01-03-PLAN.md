---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - RFMT-02

must_haves:
  truths:
    - "A developer opening CLAUDE.md immediately knows which components AI is allowed to use and that all rule details are in .claude/rules/"
    - "CLAUDE.md is under 80 lines and contains only: project description, @import directives, and 5-8 universal constraints"
    - "CLAUDE.md uses @.claude/rules/components.md and @.claude/rules/tokens.md import syntax to load the domain rules"
    - "No specific component names, no token names, no inline rule blocks appear in CLAUDE.md (those live in the scoped files)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Root rule file — project description + import directives + universal constraints"
      contains: "@.claude/rules/components.md"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/rules/components.md"
      via: "@import directive"
      pattern: "@.claude/rules/components.md"
    - from: "CLAUDE.md"
      to: ".claude/rules/tokens.md"
      via: "@import directive"
      pattern: "@.claude/rules/tokens.md"
---

<objective>
Author the CLAUDE.md root rule file that ties the entire foundation together.

Purpose: CLAUDE.md is the entry point Claude Code reads at every session start. It must be short enough to leave room for the real work, while pointing AI to the domain-scoped rule files that contain the substance. This is the manifestation of RFMT-02: modular structure with CLAUDE.md as the lightweight hub.

Output:
- `CLAUDE.md` (project root) — Under 80 lines. Contains project identity, @import directives for components.md and tokens.md, and 5-8 universal constraints that apply everywhere regardless of which files are open.

Note: There is already a CLAUDE.md at the project root containing gsd planning context. That content should be REPLACED with the actual rule document for this project. The planning artifacts live in `.planning/` — CLAUDE.md is the AI rule file, not the project planning file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key decisions the executor MUST honor. Source: 01-CONTEXT.md -->

D-11: Modular structure: CLAUDE.md (overview, ~80 lines max) + .claude/rules/*.md (domain-scoped rules).
D-12: Claude Code only — no .cursorrules, no AGENTS.md.
D-13: Rule file budget: ~80 lines for CLAUDE.md specifically. Under 80 is the hard target.
D-14: Imperative English tone.
D-16: Every critical rule includes inline WHY comment.

Claude Code @import syntax (verified official docs):
  @.claude/rules/components.md
  @.claude/rules/tokens.md

These directives cause Claude Code to load the referenced file as additional context.

<!-- What CLAUDE.md must NOT contain (kept in scoped rules files) -->
- No component names or interface definitions (in components.md)
- No token names or CSS variable lists (in tokens.md)
- No page skeleton templates (in Phase 3)
- No form structure rules (in Phase 2)
- No forbidden pattern examples with code blocks (in Phase 2)

<!-- What CLAUDE.md MUST contain -->
1. One-paragraph project description (what this codebase is, who AI is)
2. @import directives for the two rule files created in Plans 01 and 02
3. Universal constraints (5-8 max) that apply everywhere, regardless of file type:
   - Import source constraint: Composed only
   - Token usage constraint: CSS vars only, no hardcoded values
   - New component constraint: propose before creating
   - Style constraint: no inline style={{}}
   - Line count self-check reminder
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CLAUDE.md with the lean root rule file</name>
  <files>CLAUDE.md</files>

  <read_first>
    - CLAUDE.md (the current file — read first to understand what exists before overwriting)
    - .planning/phases/01-foundation/01-RESEARCH.md (section: "CLAUDE.md Root File (Phase 1 Template)", "Pitfall 1: Rule File Exceeds Token Budget")
    - .planning/phases/01-foundation/01-CONTEXT.md (decision D-11, D-12, D-13)
  </read_first>

  <action>
The current CLAUDE.md at the project root contains gsd planning infrastructure content. It must be REPLACED with the actual AI rule document for this project.

Write `CLAUDE.md` with this exact structure (adapt prose, keep structure identical):

```markdown
# Dashboard Rules

This project uses shadcn/ui with a 3-tier component hierarchy enforced by rule documents.
You are building a dashboard UI. Follow these rules for every file you touch.

@.claude/rules/components.md
@.claude/rules/tokens.md

## Always Apply

- **Imports**: Use ONLY `@/components/composed/` for UI components. Never import from `@/components/ui/` in page or feature files.
  // WHY: Direct primitive imports bypass all layout and consistency constraints.

- **Tokens**: Use CSS custom property tokens for ALL color, spacing, and radius values. Never hardcode hex, rgb, or oklch literals.
  // WHY: Hardcoded values break theming and make dark mode impossible to maintain.

- **No inline styles**: Never use `style={{}}` on any element.
  // WHY: Inline styles bypass the token system and are impossible to audit automatically.

- **New components**: Before creating a new UI component, propose it. Wait for approval. Create only in `@/components/composed/` with typed props and no className passthrough.
  // WHY: Unauthorized components fragment the system and make rules unenforceable.

- **Rule files**: When in doubt about what is allowed, read `.claude/rules/components.md` for component rules and `.claude/rules/tokens.md` for token rules. Do not infer — look it up.
```

Hard rules for this file:
- `wc -l CLAUDE.md` must return 30 or fewer (this file's job is to delegate, not contain)
- No code blocks with component interfaces — those live in components.md
- No token value lists — those live in tokens.md
- The two @import lines are MANDATORY and must appear verbatim:
  `@.claude/rules/components.md`
  `@.claude/rules/tokens.md`
- Do NOT add any rule that is already covered by the scoped rule files — CLAUDE.md only adds the meta-level constraints that apply to everything

After writing, verify line count: `wc -l CLAUDE.md`
  </action>

  <verify>
    <automated>wc -l /Users/eyelove/workspace/opensource/shadcn-rules/CLAUDE.md && grep "@.claude/rules/" /Users/eyelove/workspace/opensource/shadcn-rules/CLAUDE.md</automated>
  </verify>

  <acceptance_criteria>
    - `CLAUDE.md` exists at project root
    - `wc -l CLAUDE.md` returns 40 or fewer (strict: aim for 30, accept up to 40)
    - `grep "@.claude/rules/components.md" CLAUDE.md` finds the import directive
    - `grep "@.claude/rules/tokens.md" CLAUDE.md` finds the import directive
    - `grep "WHY" CLAUDE.md | wc -l` returns 3 or more (WHY comments on universal constraints)
    - `grep "interface\|className\|oklch\|--chart\|--kpi" CLAUDE.md` returns 0 results (no component or token specifics in root file)
    - `grep "@/components/composed/" CLAUDE.md` finds the universal import rule
    - `grep "style={{" CLAUDE.md` finds the inline style prohibition (or confirms it's mentioned)
    - File does NOT contain the previous gsd planning infrastructure (no "GSD:project-start" markers)
  </acceptance_criteria>

  <done>CLAUDE.md is the lean root rule file: under 40 lines, imports both scoped rule files via @directives, and contains only the 5 universal constraints that apply everywhere. No component interfaces, no token lists, no code blocks.</done>
</task>

</tasks>

<verification>
Final Phase 1 Foundation check — run all of these after Plan 03 completes:

```bash
# CLAUDE.md line count
wc -l CLAUDE.md

# Import directives present
grep "@.claude/rules/" CLAUDE.md

# Rule file line counts
wc -l .claude/rules/tokens.md
wc -l .claude/rules/components.md

# Token count bounded
grep -c "^    --" tokens/globals.css

# No hsl format in tokens
grep "hsl(" tokens/globals.css && echo "FAIL" || echo "PASS"

# className absent from all composed interface definitions
grep -r "className" .claude/rules/ && echo "REVIEW NEEDED" || echo "PASS"

# All 12 interfaces present
grep "interface.*Props" .claude/rules/components.md | wc -l

# Dashboard extension tokens present
grep "kpi-bg\|kpi-positive\|kpi-negative\|table-row-hover\|chart-6" tokens/globals.css | wc -l
```

Expected results:
- CLAUDE.md: ≤ 40 lines
- tokens.md: ≤ 120 lines
- components.md: ≤ 200 lines
- globals.css: 30-45 tokens
- No hsl: PASS
- className audit: PASS (0 results in prop definitions)
- Interface count: 12
- Extension tokens: 5 results
</verification>

<success_criteria>
Phase 1 Foundation is complete when:
1. A developer reads CLAUDE.md and immediately knows: (a) imports from @/components/composed/ only, (b) CSS variable tokens only, (c) no inline styles, (d) where to find detailed rules
2. CLAUDE.md is under 40 lines and imports both scoped rule files
3. The complete token vocabulary is defined in tokens/globals.css (oklch, single-hop)
4. The complete Composed component API is defined in .claude/rules/components.md (all 12 interfaces, no className)
5. Every rule file has WHY comments and escape hatches
6. All file size budgets respected (CLAUDE.md ≤ 40, tokens.md ≤ 120, components.md ≤ 200)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` following the summary template.
</output>
