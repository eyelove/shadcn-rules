---
phase: 02-rule-content
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - .claude/rules/naming.md
  - CLAUDE.md
  - scripts/check-rules.sh
autonomous: true
requirements:
  - NAME-01
  - NAME-02
  - NAME-03

must_haves:
  truths:
    - "Given any component, page, or CSS file name, a developer can verify it against the naming rules without ambiguity"
    - "CLAUDE.md imports all three new rule files via @import directives"
    - "check-rules.sh includes new checks covering FORB-05 and additional forbidden patterns"
    - "File naming, directory structure, component naming, and CSS variable naming conventions are all documented with examples"
    - "The barrel export pattern from @/components/composed is reinforced in naming.md"
  artifacts:
    - path: ".claude/rules/naming.md"
      provides: "File naming, directory structure, component naming, CSS variable naming rules"
      exports: ["NAME-01 file naming", "NAME-02 component naming", "NAME-03 CSS class/variable naming"]
    - path: "CLAUDE.md"
      provides: "Root rule file updated with @import for forbidden.md, forms.md, naming.md"
    - path: "scripts/check-rules.sh"
      provides: "Automated checker extended with new checks from forbidden.md patterns"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/rules/forbidden.md"
      via: "@.claude/rules/forbidden.md import directive"
      pattern: "@.claude/rules/forbidden.md"
    - from: "CLAUDE.md"
      to: ".claude/rules/forms.md"
      via: "@.claude/rules/forms.md import directive"
      pattern: "@.claude/rules/forms.md"
    - from: "CLAUDE.md"
      to: ".claude/rules/naming.md"
      via: "@.claude/rules/naming.md import directive"
      pattern: "@.claude/rules/naming.md"
---

<objective>
Three integration tasks that complete Phase 2:
1. Create `.claude/rules/naming.md` — file naming, directory structure, component naming, CSS variable naming conventions
2. Update `CLAUDE.md` — add @import directives for the three new Phase 2 rule files
3. Extend `scripts/check-rules.sh` — add checks for patterns documented in forbidden.md

Purpose: After plans 01 and 02, the rules exist but are not reachable from CLAUDE.md and are not checked by automation. This plan wires everything together so AI can navigate to all rules from one entry point, and the checker validates the new rules automatically.

Output:
- `.claude/rules/naming.md` (new file, ~90 lines)
- `CLAUDE.md` (modified — 3 new @import lines added)
- `scripts/check-rules.sh` (modified — new checks appended)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-rule-content/02-CONTEXT.md
@CLAUDE.md
@scripts/check-rules.sh
@.planning/phases/02-rule-content/02-01-SUMMARY.md
@.planning/phases/02-rule-content/02-02-SUMMARY.md

<interfaces>
<!-- Naming decisions from 02-CONTEXT.md D-10 through D-16 -->

File naming rules (D-11):
- Page files: kebab-case  e.g. campaign-list.tsx, campaign-form.tsx
- Composed components: PascalCase  e.g. DataTable.tsx, SearchBar.tsx
- Hook files: camelCase with use prefix  e.g. useFilters.ts, usePagination.ts
- Type files: PascalCase  e.g. Campaign.ts, AdGroup.ts

Directory structure (D-12):
- components/ui/       shadcn primitives (auto-managed, do not edit)
- components/composed/ project wrappers (AI creates here)
- app/ or pages/       page files (framework-dependent)
- hooks/               custom hooks
- types/               shared type definitions
- tokens/              CSS custom property files

Barrel export pattern (D-16): @/components/composed barrel export must be reinforced

CLAUDE.md current content (24 lines, 3 @import lines):
Line 7: @.claude/rules/components.md
Line 8: @.claude/rules/component-interfaces.md
Line 9: @.claude/rules/tokens.md

New @imports to add after line 9:
@.claude/rules/forbidden.md
@.claude/rules/forms.md
@.claude/rules/naming.md

check-rules.sh current checks (15 checks in the script):
The script uses bash check() and check_absent() functions.
New checks follow the pattern: check "Description" 'grep-pattern' "$TARGET"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claude/rules/naming.md with NAME-01, NAME-02, NAME-03</name>
  <files>.claude/rules/naming.md</files>
  <read_first>
    - .planning/phases/02-rule-content/02-CONTEXT.md (D-10 through D-16 — all naming decisions)
    - .claude/rules/tokens.md (frontmatter format and writing style to match exactly)
    - tests/samples/campaign-form.tsx (verify file naming conventions match the existing samples)
  </read_first>
  <action>
Create `.claude/rules/naming.md` with this exact content:

---
paths:
  - "src/**/*.tsx"
  - "src/**/*.ts"
  - "src/**/*.css"
  - "app/**/*.tsx"
  - "app/**/*.ts"
  - "components/**/*.tsx"
  - "components/**/*.ts"
  - "hooks/**/*.ts"
  - "types/**/*.ts"
  - "tokens/**/*.css"
---

# Naming Conventions

## NAME-01 — File Naming

| File Type | Convention | Examples |
|-----------|-----------|---------|
| Page files | kebab-case | campaign-list.tsx, campaign-form.tsx, ad-group-detail.tsx |
| Composed components | PascalCase | DataTable.tsx, SearchBar.tsx, FormFieldSet.tsx |
| Hook files | camelCase with use prefix | useFilters.ts, usePagination.ts, useCampaignData.ts |
| Type files | PascalCase | Campaign.ts, AdGroup.ts, FilterConfig.ts |
| CSS token files | kebab-case | globals.css, dashboard-tokens.css |

// WHY: Convention-per-type makes file intent obvious without opening the file. AI infers
// where to create new files based on type alone — no ambiguity about page vs component files.

## NAME-02 — Component Naming

Composed components MUST follow these naming patterns:

| Pattern | Use For | Examples |
|---------|---------|---------|
| Noun | Single-purpose display | DataTable, StatusBadge, PageHeader |
| NounGroup | Multi-item container | KpiCardGroup, ChartSection |
| FormNoun | Form-specific | FormField, FormFieldSet, FormRow, FormActions |
| ActionNoun | Interactive | ActionButton, ConfirmDialog |

NEVER use a UI suffix or Base prefix (UIButton, BaseCard).
// WHY: UI/Base prefixes are shadcn primitive naming conventions. Using them implies
// the component is a primitive, not a Composed wrapper — this breaks the tier model.

Barrel export — all Composed components MUST be exported from @/components/composed/index.ts:

    // CORRECT — barrel import
    import { DataTable, FormField, ActionButton } from "@/components/composed"

    // FORBIDDEN — direct file import bypassing barrel
    import { DataTable } from "@/components/composed/DataTable"

// WHY: The barrel is a single choke point. Every new component must be registered here
// to be usable. All 4 Phase 1 test samples confirmed consistent barrel usage.

## NAME-03 — CSS Variable and Class Naming

CSS custom properties (tokens):
- Dashboard extension tokens use prefixes: --kpi-*, --chart-*, --table-*
- All tokens defined in tokens/globals.css — NEVER define CSS variables in component files
// WHY: Centralized token definition makes theme overrides a single-file operation.

Tailwind class usage:
- Layout and spacing: flex, grid, gap-4, p-6 (Tailwind utilities — allowed)
- Color: bg-background, text-foreground, border-border (token-aliased — required)
- NEVER: bg-zinc-900, text-gray-500 (raw Tailwind color primitives — hardcode the value)

CSS module file naming (if used): ComponentName.module.css — matches the component file name.

## Directory Structure

    components/
      ui/          <- shadcn primitives — DO NOT edit or import directly
      composed/    <- AI creates new components here ONLY
    app/ or pages/ <- page files (framework-dependent)
    hooks/         <- custom React hooks (useX naming required)
    types/         <- shared TypeScript types
    tokens/        <- CSS custom property files

## Escape Hatch

If the naming rule creates an ambiguous case:
1. Apply the convention that best reflects the file PRIMARY purpose
2. Document the reasoning in a comment at the top of the file
3. NEVER deviate from directory placement rules — directory determines tier

The file content should be formatted as proper Markdown with YAML frontmatter, using backticks for inline code in the actual file. Keep it under 90 lines.
  </action>
  <verify>
    <automated>grep -c "NAME-0[1-3]" /Users/eyelove/workspace/opensource/shadcn-rules/.claude/rules/naming.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "NAME-01" .claude/rules/naming.md returns a match
    - grep "NAME-02" .claude/rules/naming.md returns a match
    - grep "NAME-03" .claude/rules/naming.md returns a match
    - grep "kebab-case" .claude/rules/naming.md returns a match (file naming rule present)
    - grep "PascalCase" .claude/rules/naming.md returns a match (component naming present)
    - grep "useFilters\|use prefix" .claude/rules/naming.md returns a match (hook naming present)
    - grep "barrel\|Barrel" .claude/rules/naming.md returns a match (D-16 barrel pattern reinforced)
    - grep "// WHY:" .claude/rules/naming.md returns 3 or more lines
    - grep "FORBIDDEN\|NEVER" .claude/rules/naming.md returns a match
    - grep "Escape Hatch" .claude/rules/naming.md returns a match
    - grep "^paths:" .claude/rules/naming.md returns a match (frontmatter present)
    - wc -l .claude/rules/naming.md returns 100 or fewer
  </acceptance_criteria>
  <done>naming.md exists with NAME-01 (file naming table with 5 types), NAME-02 (Composed naming patterns + barrel export rule), NAME-03 (CSS variable and class naming), directory structure reference, and Escape Hatch. File is under 90 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md imports and extend check-rules.sh</name>
  <files>CLAUDE.md, scripts/check-rules.sh</files>
  <read_first>
    - CLAUDE.md (full file — 24 lines, @import section ends at line 9 with @.claude/rules/tokens.md)
    - scripts/check-rules.sh (full file — understand existing check() function format before adding checks)
  </read_first>
  <action>
**CLAUDE.md update:**

Read the current CLAUDE.md (24 lines). After the line `@.claude/rules/tokens.md`, insert three new lines:

    @.claude/rules/forbidden.md
    @.claude/rules/forms.md
    @.claude/rules/naming.md

The result should have 6 @import lines in the file header block. Keep the "## Always Apply" section and all its content completely unchanged. The final CLAUDE.md should be 27 lines total.

**check-rules.sh extension:**

Read the current check-rules.sh. After the last existing `check` call (the rounded-md/lg check on the line containing "rounded-\(md\|lg\|xl\|2xl\|full\)"), and BEFORE the blank line that leads to "--- REQUIRED PATTERNS ---", insert these 3 new check calls:

    # 14. FORB-05 — Standalone Textarea outside FormField
    check "No standalone Textarea (use FormField > Textarea)" '<Textarea[^/]*name=' "$TARGET"

    # 15. FORB-05 — Standalone Checkbox outside FormField
    check "No standalone Checkbox (use FormField > Checkbox)" '<Checkbox[^/]*name=' "$TARGET"

    # 16. Tailwind fixed rounded-sm (should use token)
    check "No Tailwind fixed rounded-sm (use token)" 'rounded-sm' "$TARGET"

After writing the file, verify bash syntax: bash -n scripts/check-rules.sh
The command must exit 0 (no syntax errors).
  </action>
  <verify>
    <automated>grep "@.claude/rules/forbidden.md" /Users/eyelove/workspace/opensource/shadcn-rules/CLAUDE.md && grep "@.claude/rules/forms.md" /Users/eyelove/workspace/opensource/shadcn-rules/CLAUDE.md && grep "@.claude/rules/naming.md" /Users/eyelove/workspace/opensource/shadcn-rules/CLAUDE.md && bash -n /Users/eyelove/workspace/opensource/shadcn-rules/scripts/check-rules.sh && echo "syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep "@.claude/rules/forbidden.md" CLAUDE.md returns a match
    - grep "@.claude/rules/forms.md" CLAUDE.md returns a match
    - grep "@.claude/rules/naming.md" CLAUDE.md returns a match
    - grep -c "@.claude/rules" CLAUDE.md returns 6 (was 3, now 6)
    - grep "## Always Apply" CLAUDE.md returns a match (section preserved)
    - grep "Textarea" scripts/check-rules.sh returns a match (new check present)
    - grep "Checkbox" scripts/check-rules.sh returns a match (new check present)
    - grep "rounded-sm" scripts/check-rules.sh returns a match (new check present)
    - bash -n scripts/check-rules.sh exits 0 (valid bash syntax)
    - wc -l CLAUDE.md returns 27 (was 24, +3 lines)
  </acceptance_criteria>
  <done>CLAUDE.md has 6 @import lines (3 original + forbidden.md + forms.md + naming.md). check-rules.sh has 3 new check() calls for FORB-05 patterns and rounded-sm. Script passes bash syntax check.</done>
</task>

</tasks>

<verification>
Run from project root:

    # Confirm naming.md exists with all 3 NAME rules
    grep "NAME-0[1-3]" .claude/rules/naming.md
    # Expected: 3 matches

    # Confirm CLAUDE.md has all 6 imports
    grep "@.claude/rules" CLAUDE.md
    # Expected: 6 lines

    # Confirm check-rules.sh is valid bash and has new checks
    bash -n scripts/check-rules.sh && grep -c "^check " scripts/check-rules.sh
    # Expected: no error, count should be 16+ (was 13)

    # Run checker against test samples to confirm no false positives
    bash scripts/check-rules.sh tests/samples
    # Expected: 0 violations (samples should still pass)
</verification>

<success_criteria>
- `.claude/rules/naming.md` exists with NAME-01 (file naming), NAME-02 (component naming + barrel export), NAME-03 (CSS variable naming), directory structure, and Escape Hatch
- `CLAUDE.md` imports all 6 rule files including the 3 new Phase 2 files
- `scripts/check-rules.sh` has 3 new check() calls; passes `bash -n` syntax check
- Running `bash scripts/check-rules.sh tests/samples` produces 0 violations (existing test samples still pass all checks)
</success_criteria>

<output>
After completion, create `.planning/phases/02-rule-content/02-03-SUMMARY.md`
</output>
