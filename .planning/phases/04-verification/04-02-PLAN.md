---
phase: 04-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/samples/campaign-list.tsx
  - tests/samples/campaign-detail.tsx
  - tests/samples/campaign-form.tsx
  - tests/samples/dashboard-overview.tsx
autonomous: true
requirements:
  - VERF-02
  - VERF-05
must_haves:
  truths:
    - "A sample page exists for the List page type (campaign-list.tsx) generated in a fresh AI context"
    - "A sample page exists for the Detail page type (campaign-detail.tsx) generated in a fresh AI context"
    - "A sample page exists for the Form/Settings page type (campaign-form.tsx) generated in a fresh AI context"
    - "A sample page exists for the Dashboard Overview page type (dashboard-overview.tsx) generated in a fresh AI context"
    - "Each sample was generated by a separate subagent that read only the 8 rule files — not the context window that spawned it"
  artifacts:
    - path: "tests/samples/campaign-list.tsx"
      provides: "List page sample per PAGE-01"
      contains: "PageLayout"
    - path: "tests/samples/campaign-detail.tsx"
      provides: "Detail page sample per PAGE-02"
      contains: "KpiCardGroup"
    - path: "tests/samples/campaign-detail.tsx"
      provides: "Detail page uses flat structure"
      contains: "ChartSection"
    - path: "tests/samples/campaign-form.tsx"
      provides: "Form page sample per PAGE-03"
      contains: "FormFieldSet"
    - path: "tests/samples/dashboard-overview.tsx"
      provides: "Dashboard sample per PAGE-04"
      contains: "ChartSection"
  key_links:
    - from: "tests/samples/*.tsx"
      to: "scripts/check-rules.sh"
      via: "bash scripts/check-rules.sh tests/samples/"
      pattern: "PageLayout"
---

<objective>
Delete the 4 existing sample pages (generated before Phase 2+3 rules existed) and regenerate them using the complete rule set. Each sample MUST be generated by a SEPARATE subagent with a FRESH context window — the executor must NOT generate the samples itself.

Purpose: Samples generated without Phase 2+3 rules may contain violations. Fresh-context generation proves the rules work in isolation (VERF-05).
Output: 4 new sample TSX files in tests/samples/, each conforming to the complete 8-file rule set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@.claude/rules/components.md
@.claude/rules/component-interfaces.md
@.claude/rules/tokens.md
@.claude/rules/forbidden.md
@.claude/rules/forms.md
@.claude/rules/naming.md
@.claude/rules/page-templates.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old samples</name>
  <files>tests/samples/campaign-list.tsx, tests/samples/campaign-detail.tsx, tests/samples/campaign-form.tsx, tests/samples/dashboard-overview.tsx</files>
  <read_first>
    - tests/samples/ — confirm exactly which files exist before deleting
  </read_first>
  <action>
Delete all 4 existing sample files in tests/samples/:
- tests/samples/campaign-list.tsx
- tests/samples/campaign-detail.tsx
- tests/samples/campaign-form.tsx
- tests/samples/dashboard-overview.tsx

Use: `rm tests/samples/campaign-list.tsx tests/samples/campaign-detail.tsx tests/samples/campaign-form.tsx tests/samples/dashboard-overview.tsx`

Do NOT delete the tests/samples/ directory itself — the directory must remain for the subagents to write into.

After deletion, confirm: `ls tests/samples/` returns empty or only non-TSX files.

Per D-05: Old samples predate Phase 2+3 rules and MUST be regenerated from scratch.
  </action>
  <acceptance_criteria>
    - `ls tests/samples/*.tsx 2>/dev/null | wc -l` returns 0 (no TSX files remain)
    - `ls tests/samples/` does not error (directory still exists)
  </acceptance_criteria>
  <verify>
    <automated>ls /Users/eyelove/workspace/opensource/shadcn-rules/tests/samples/</automated>
  </verify>
  <done>All 4 old sample TSX files are deleted; tests/samples/ directory is empty</done>
</task>

<task type="auto">
  <name>Task 2: Spawn 4 parallel subagents to regenerate sample pages</name>
  <files>tests/samples/campaign-list.tsx, tests/samples/campaign-detail.tsx, tests/samples/campaign-form.tsx, tests/samples/dashboard-overview.tsx</files>
  <read_first>
    - CLAUDE.md — the hub that imports all 8 rule files; this is the ONLY context the subagents need
    - .claude/rules/page-templates.md — understand the 4 page type templates
  </read_first>
  <action>
**CRITICAL: The executor must NOT generate the sample pages itself. Spawn 4 separate subagents in parallel — one per page type. Each subagent starts with a fresh context.**

Spawn 4 parallel Task tool invocations (or equivalent agent spawning mechanism), one per page type. Each subagent receives ONLY this prompt (customized per page type):

---

**Subagent prompt for campaign-list.tsx:**
```
You are a fresh context agent. Read only these rule files — do not read any other files:
- CLAUDE.md
- .claude/rules/components.md
- .claude/rules/component-interfaces.md
- .claude/rules/tokens.md
- .claude/rules/forbidden.md
- .claude/rules/forms.md
- .claude/rules/naming.md
- .claude/rules/page-templates.md

After reading all 8 files, generate a sample List page following PAGE-01 exactly.
The page models a "Campaigns" list.

Write the output to: tests/samples/campaign-list.tsx

Requirements:
- Follow PAGE-01 skeleton: PageLayout → PageHeader → SearchBar → DataTable
- Use barrel import: import { ... } from "@/components/composed"
- No KpiCardGroup, no ChartSection, no TabGroup
- No inline styles, no hardcoded hex colors, no direct @/components/ui/ imports
- Include realistic mock data (5-10 rows) as a const above the component
- Use named export: export default function CampaignListPage()
- File must be self-contained TSX (no external imports beyond @/components/composed and React)
```

---

**Subagent prompt for campaign-detail.tsx:**
```
You are a fresh context agent. Read only these rule files — do not read any other files:
- CLAUDE.md
- .claude/rules/components.md
- .claude/rules/component-interfaces.md
- .claude/rules/tokens.md
- .claude/rules/forbidden.md
- .claude/rules/forms.md
- .claude/rules/naming.md
- .claude/rules/page-templates.md

After reading all 8 files, generate a sample Detail page following PAGE-02 exactly.
The page models a "Campaign Detail" view.

Write the output to: tests/samples/campaign-detail.tsx

Requirements:
- Follow PAGE-02 skeleton: PageLayout → PageHeader(backHref, action=StatusBadge) → KpiCardGroup → ChartSection → DataTable
- Flat structure — NO TabGroup
- ChartSection must have cols={1} (detail page uses single column)
- PageHeader must have backHref="/campaigns" and action={<StatusBadge status="active" />}
- Use barrel import: import { ... } from "@/components/composed"
- No inline styles, no hardcoded hex colors, no direct @/components/ui/ imports
- Include realistic mock KPI items (4 items) and mock table rows
- Use named export: export default function CampaignDetailPage()
```

---

**Subagent prompt for campaign-form.tsx:**
```
You are a fresh context agent. Read only these rule files — do not read any other files:
- CLAUDE.md
- .claude/rules/components.md
- .claude/rules/component-interfaces.md
- .claude/rules/tokens.md
- .claude/rules/forbidden.md
- .claude/rules/forms.md
- .claude/rules/naming.md
- .claude/rules/page-templates.md

After reading all 8 files, generate a sample Form/Settings page following PAGE-03 exactly.
The page models a "Create Campaign" form.

Write the output to: tests/samples/campaign-form.tsx

Requirements:
- Follow PAGE-03 skeleton: PageLayout → PageHeader(backHref) → form → FormFieldSet(s) → FormActions
- PageHeader must have backHref="/campaigns"
- FormActions: Cancel (variant="outline") BEFORE Save (type="submit")
- FormActions must be OUTSIDE and AFTER FormFieldSet (sibling, not child)
- Use at least 2 FormFieldSets with 2+ fields each
- Use FormRow cols={2} for side-by-side fields
- No raw <input>, <label>, or <div> layout elements
- Use barrel import: import { ... } from "@/components/composed"
- No inline styles, no hardcoded hex colors
- Use named export: export default function CampaignFormPage()
```

---

**Subagent prompt for dashboard-overview.tsx:**
```
You are a fresh context agent. Read only these rule files — do not read any other files:
- CLAUDE.md
- .claude/rules/components.md
- .claude/rules/component-interfaces.md
- .claude/rules/tokens.md
- .claude/rules/forbidden.md
- .claude/rules/forms.md
- .claude/rules/naming.md
- .claude/rules/page-templates.md

After reading all 8 files, generate a sample Dashboard Overview page following PAGE-04 exactly.
The page models a marketing dashboard.

Write the output to: tests/samples/dashboard-overview.tsx

Requirements:
- Follow PAGE-04 skeleton: PageLayout → PageHeader → KpiCardGroup → ChartSection(cols=2) → DataTable
- ChartSection MUST have cols={2} — this is a hard PAGE-04 requirement
- KpiCardGroup must have cols={4} and at least 4 KPI items
- Include at least 2 chart entries in ChartSection
- Use barrel import: import { ... } from "@/components/composed"
- No inline styles, no hardcoded hex colors, no direct @/components/ui/ imports
- Include mock data for KPIs and table
- Use named export: export default function DashboardOverviewPage()
```

---

After all 4 subagents complete, verify all 4 files exist:
`ls tests/samples/*.tsx` — must list all 4 files.

Per D-06: Each sample generated by a subagent with fresh context. This satisfies VERF-05 (no self-evaluation — generation and evaluation are in separate contexts).
  </action>
  <acceptance_criteria>
    - `ls tests/samples/campaign-list.tsx` exits 0
    - `ls tests/samples/campaign-detail.tsx` exits 0
    - `ls tests/samples/campaign-form.tsx` exits 0
    - `ls tests/samples/dashboard-overview.tsx` exits 0
    - `grep 'PageLayout' tests/samples/campaign-list.tsx` returns a match
    - `grep 'KpiCardGroup' tests/samples/campaign-detail.tsx` returns a match
    - `grep 'FormFieldSet' tests/samples/campaign-form.tsx` returns a match
    - `grep 'ChartSection cols={2}' tests/samples/dashboard-overview.tsx` returns a match
    - `grep 'TabGroup' tests/samples/campaign-detail.tsx` returns no match (flat structure)
    - `grep 'from.*@/components/ui/' tests/samples/*.tsx` returns no matches
    - `grep 'style={{' tests/samples/*.tsx` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>ls /Users/eyelove/workspace/opensource/shadcn-rules/tests/samples/*.tsx && grep -l 'PageLayout' /Users/eyelove/workspace/opensource/shadcn-rules/tests/samples/*.tsx | wc -l</automated>
  </verify>
  <done>4 sample TSX files exist in tests/samples/, each generated by a separate fresh-context subagent, each following its page type template exactly</done>
</task>

</tasks>

<verification>
Run: `bash scripts/check-rules.sh tests/samples/`
- All 4 samples must produce minimal violations (ideally 0)
- If violations appear, they are documented and deferred to gap closure — do NOT fix samples during this plan
Run: `grep 'from.*@/components/composed"' tests/samples/*.tsx`
- All 4 files must import from barrel, not direct paths
</verification>

<success_criteria>
- 4 TSX files exist in tests/samples/ (replacing the deleted old ones)
- Each sample was generated by a fresh-context subagent (VERF-05 satisfied by design)
- Each sample follows its page type skeleton template
- No direct @/components/ui/ imports, no inline styles, no hardcoded hex colors in any sample
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification/04-02-SUMMARY.md` using the template at `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
